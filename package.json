{
  "name": "bidfootball",
  "version": "1.0.0",
  "private": true,
  "description": "Bid to crown the top football club of every country. bidfootball.lol",
  "scripts": {
    "dev": "wrangler pages dev public --kv BIDS",
    "deploy": "wrangler pages deploy public",
    "kv:create": "wrangler kv namespace create BIDS && wrangler kv namespace create BIDS --preview",
    "tail": "wrangler pages deployment tail"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "wrangler": "^4.99.0"
  }
}
